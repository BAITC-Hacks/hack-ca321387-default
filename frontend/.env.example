# Real API is the default. Mock fixtures are allowed only in development.
VITE_USE_MOCK_API=false
# Backend origin (http://localhost:8000) or API base (/api) are both accepted.
VITE_API_URL=/api

# Development-only switch for exercising the chat message error and retry state.
VITE_CHAT_MOCK_ERROR=false
